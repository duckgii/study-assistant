{$DOMAIN:localhost} {
	encode gzip

	# PDF / PPTX uploads
	request_body {
		max_size 100MB
	}

	reverse_proxy app:3000 {
		header_up X-Forwarded-Proto {scheme}
		header_up X-Forwarded-Host {host}
		flush_interval -1
	}
}
